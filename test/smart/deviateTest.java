/*package smart;
import org.junit.Test;
import org.hamcrest.*;
import static org.junit.Assert.*;

public class deviateTest {
    float in = 2f;
    float s = 2.5f
    deviate  d = new deviate(in,s);
    //Getter testing
    @Test
    public void getDeviate()
    {
        assertEquals(in, d.getUserInput() );
        assertNotEquals(s, d.getSuggested());
    }

    //Setter testing
    @Test

    public void setDeviate()
    {
        in = 3f;
        s = 3.5f;
        d.setUserInput(in);
        d.setSuggested(s);
        assertEquals(in, d.getUserInput() );
        assertNotEquals(s, d.getSuggested());
    }


}
*/